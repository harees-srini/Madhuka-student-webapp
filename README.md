# Madhuka-student-webapp
# Libraries to install: cd server "npm install mongoose cors express"
# To open backend server (Node) cd server "npm start"
# To open frontend server (React) cd client "npm start"
